import 'package:flutter/material.dart';

class SearchHomeTab extends StatelessWidget {
  const SearchHomeTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
