import 'package:flutter/material.dart';

class NotificationsHomeTab extends StatelessWidget {
  const NotificationsHomeTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
